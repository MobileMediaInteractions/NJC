import type { FeatureDiagnostic } from "@visual-feature/model";

export function ComposerCode({ source, diagnostics, onSource, onApply, onFormat }: { source: string; diagnostics: FeatureDiagnostic[]; onSource: (value: string) => void; onApply: () => void; onFormat: () => void }) {
  return <div className="composer-code"><section><div className="graph-head"><span>READABLE FEATURE SOURCE</span><small>Plain English flow · typed programming primitives · deterministic</small><button type="button" onClick={onFormat}>Format</button><button type="button" className="apply-source" onClick={onApply}>Apply to visual model</button></div><div className="code-gutter" aria-hidden="true">{source.split("\n").map((_, index) => <span key={index + 1}>{index + 1}</span>)}</div><textarea aria-label="Readable feature source" spellCheck={false} value={source} onChange={(event) => onSource(event.target.value)} /></section><aside><div className="composer-panel-title"><span>LANGUAGE STATUS</span><small>{diagnostics.length ? `${diagnostics.length} diagnostics` : "Valid"}</small></div>{diagnostics.length ? diagnostics.map((item) => <article key={`${item.code}-${item.objectId ?? item.message}`}><i className={item.severity} /> <div><strong>{item.code}</strong><p>{item.message}</p></div></article>) : <div className="language-ok"><span>✓</span><strong>Source and Feature IR agree</strong><p>Readable source parsed and type-checked into the same canonical model used by Design, Blueprints, Data, Motion and Test.</p></div>}<h3>Readable + programmable</h3><ul><li><code>Create feature</code> and <code>Show screen</code> declarations</li><li><code>When</code>, <code>If</code> and <code>Otherwise</code> control flow</li><li><code>Set</code>, <code>Call</code>, <code>Show</code> and <code>Play</code> actions</li><li>Stable IDs and typed references</li><li>Parser-backed symbols, formatting and diagnostics</li><li>Legacy controlled source remains supported</li></ul></aside></div>;
}
