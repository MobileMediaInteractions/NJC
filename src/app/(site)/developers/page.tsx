import Link from "next/link";
import { redirect } from "next/navigation";
import { Code2, Gauge, ShieldCheck } from "lucide-react";
import { ApiKeyManager } from "@/components/developers/api-key-manager"; import { Badge } from "@/components/ui/badge"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { getAccountIdentity, isClerkConfigured } from "@/lib/auth";
export const dynamic = "force-dynamic";
export default async function DevelopersPage() { if (!isClerkConfigured()) return <main id="main-content" className="container-news py-14"><Badge>Developer platform</Badge><h1 className="mt-4 text-4xl font-black text-brand-navy">Developer access is ready to connect.</h1><p className="mt-4 max-w-2xl text-muted-foreground">Connect Clerk and Postgres in Vercel to enable verified accounts and scoped API-key generation. The public mobile API continues to work without developer credentials.</p></main>; const identity = await getAccountIdentity(); if (!identity) redirect('/sign-in?redirect_url=/developers'); return <main id="main-content" className="container-news py-12"><div className="max-w-3xl"><Badge className="bg-brand-yellow text-brand-navy">Harborline Developers</Badge><h1 className="mt-4 text-4xl font-black tracking-tight text-brand-navy sm:text-5xl">Build useful local-news experiences.</h1><p className="mt-4 text-lg leading-8 text-muted-foreground">Create read-only keys for published coverage, weather and live status. Keys are hashed at rest, scoped, rate limited and fully revocable.</p></div><div className="my-8 grid gap-4 md:grid-cols-3">{[[ShieldCheck, 'Safe by default', 'No editorial write scopes are issued through self-service.'], [Gauge, 'Predictable limits', '60 requests per minute and 10,000 per day per key.'], [Code2, 'Versioned JSON', 'Stable /api/developer/v1 routes with documented envelopes.']].map(([Icon, title, body]) => <Card key={String(title)}><CardHeader><Icon className="size-5 text-primary" /><CardTitle className="text-base">{String(title)}</CardTitle></CardHeader><CardContent className="text-sm text-muted-foreground">{String(body)}</CardContent></Card>)}</div><ApiKeyManager /><section className="mt-10 rounded-lg bg-brand-navy p-6 text-white"><h2 className="text-xl font-bold">Quick start</h2><pre className="mt-4 overflow-x-auto rounded bg-black/25 p-4 text-xs"><code>{`curl -H "X-API-Key: hln_live_…" \\\n  ${process.env.NEXT_PUBLIC_SITE_URL ?? 'https://your-domain.example'}/api/developer/v1/stories`}</code></pre><p className="mt-4 text-sm text-white/70">By using the API you agree to the <Link className="font-semibold text-brand-yellow" href="/api-terms">API Terms</Link> and <Link className="font-semibold text-brand-yellow" href="/developer-agreement">Developer Agreement</Link>.</p></section></main>; }
