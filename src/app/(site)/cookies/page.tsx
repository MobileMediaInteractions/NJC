import type { Metadata } from "next"; import { InfoPage } from "@/components/info-page";
export const metadata: Metadata = { title: "Cookie policy" };
export default function CookiesPage() { return <InfoPage eyebrow="Legal · Effective July 13, 2026" title="Cookie and local-storage policy" intro="Harborline separates storage needed to operate the service from optional audience measurement."><section><h2 className="text-2xl font-black text-brand-navy">Essential storage</h2><p className="mt-3">Clerk may store authentication and fraud-prevention values. Harborline stores consent choices and interface preferences. The mobile app uses on-device SQLite key-value storage for cached news, weather and bookmarks. These functions are necessary or explicitly requested and cannot all be disabled without losing the related feature.</p></section><section><h2 className="text-2xl font-black text-brand-navy">Optional measurement</h2><p className="mt-3">Optional analytics is disabled by default in this build and must not load until a visitor chooses “Allow analytics.” If a real analytics or advertising provider is added, this policy and the consent controls must list its purpose, duration and recipients before activation.</p></section><section><h2 className="text-2xl font-black text-brand-navy">Change a choice</h2><p className="mt-3">Clear the site’s local storage or browser data to reset the consent banner. A production consent-management system should add granular withdrawal and a “Your Privacy Choices” control in the footer for applicable markets.</p></section></InfoPage>; }
